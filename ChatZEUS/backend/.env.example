# ChatZEUS Environment Configuration
# تكوين بيئة ChatZEUS

# Server Configuration
# تكوين الخادم
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database Configuration
# تكوين قاعدة البيانات
MONGODB_URI=mongodb+srv://gar86912:M1uEr2lDkKJyrrlb@zeuss.3duzako.mongodb.net/?retryWrites=true&w=majority&appName=Zeuss
MONGODB_DATABASE=chatzeus

# Security Configuration
# تكوين الأمان
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=12

# Rate Limiting
# تحديد معدل الطلبات
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AI Provider API Keys (Optional - can be added via UI)
# مفاتيح API مزودي الذكاء الاصطناعي (اختياري - يمكن إضافتها عبر الواجهة)
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
GOOGLE_API_KEY=your-google-api-key-here

# File Upload Configuration
# تكوين رفع الملفات
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
ALLOWED_FILE_TYPES=.txt,.js,.py,.html,.css,.json,.md,.ts,.jsx,.tsx,.vue,.php,.java,.cpp,.c,.go,.rs,.swift,.kt,.scala,.r,.sql,.xml,.yaml,.yml,.toml,.ini

# Logging Configuration
# تكوين التسجيل
LOG_LEVEL=info
LOG_FILE=./logs/chatzeus.log

# Email Configuration (for future use)
# تكوين البريد الإلكتروني (للاستخدام المستقبلي)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Redis Configuration (for caching and sessions)
# تكوين Redis (للتخزين المؤقت والجلسات)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# Monitoring and Analytics
# المراقبة والتحليلات
ENABLE_METRICS=true
METRICS_PORT=9090

# Development Configuration
# تكوين التطوير
DEBUG=true
ENABLE_SWAGGER=true
ENABLE_CORS=true

# Production Configuration
# تكوين الإنتاج
ENABLE_COMPRESSION=true
ENABLE_HELMET=true
ENABLE_RATE_LIMITING=true